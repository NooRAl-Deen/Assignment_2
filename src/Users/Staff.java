package Users;

public class Staff extends User {

    public Staff(int ID, String name, int age, double hourlyRate, int totalHoursPerMonth) {
        super(ID, name, age, hourlyRate, totalHoursPerMonth);
    }
}
